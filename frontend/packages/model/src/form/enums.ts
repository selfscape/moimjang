export enum SurveyRegistState {
  PENDING = "PENDING",
  ACCEPT = "ACCEPT",
  REJECT = "REJECT",
}

export enum SurveyType {
  PLAINTEXT = "PLAINTEXT",
  SELECT = "SELECT",
  DROPDOWN = "DROPDOWN",
  IMAGE = "IMAGE",
  AGREEMENT = "AGREEMENT",
}
